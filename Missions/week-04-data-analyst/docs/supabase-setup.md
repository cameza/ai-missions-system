# Supabase Configuration
# Add these to your Vercel environment variables and local .env file
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Development Notes:
# 1. Get actual keys from Supabase dashboard > Settings > API
# 2. Add these environment variables to Vercel project settings
# 3. Never commit actual keys to repository
# 4. Service role key should only be used on server-side
